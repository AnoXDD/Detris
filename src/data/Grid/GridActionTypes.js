/**
 * Created by Anoxic on 9/21/2017.
 *
 * An enum class to list the kind of actions grid will execute
 */

const ActionTypes = {
  // Initialize the grid
  INIT          : "INIT",
  // Drop a detromino in the grid
  DROP_DETROMINO: "DROP_DETROMINO",
};

export default ActionTypes;
